import { MobileDirective } from './mobile.directive';

describe('MobileDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileDirective();
    expect(directive).toBeTruthy();
  });
});
